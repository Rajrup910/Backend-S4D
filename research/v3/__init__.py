"""V3 -- breaking the 0.80 ceiling by moving the representation, not the decision layer.

Phase A (S40) is validity triage: two leads that motivated V2's Track B are tested and,
if they do not survive, killed in writing.

    oos_probe.py            N2 kill test -- was S35's 0.997 a representation finding or a leak?
    extract_oof_features.py the honest OOF feature panel Stage 1 needs
    efficiency_audit.py     re-read of S34's "under-40 is the least efficient band"

No module here reads the HAM test split. `results/test_pass_receipt.json` stays at
`n_executions: 2` until the Phase E gate fires.
"""
